#!/usr/bin/env bash
set -e

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export NODE_PATH="${ROOT_DIR}/node_modules:${HOME}/.npm-global/lib/node_modules:${NODE_PATH}"

node "${ROOT_DIR}/.agents/skills/zhakyz-pdf/scripts/assemble.js" "$@"
